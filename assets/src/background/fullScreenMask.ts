/* 全屏半透明遮罩 */
import { _decorator, Component, Node, Graphics } from 'cc';
const { ccclass, property } = _decorator;

@ccclass('FullScreenMask')
export class FullScreenMask extends Component {
    start() {
    }

    update(deltaTime: number) {
        
    }
}


